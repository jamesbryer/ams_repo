on:
  workflow_dispatch:
jobs:
  my_job:
    name: deploy to staging
    runs-on: ubuntu-20.04
    steps:
    - name: Install sl
      run: sudo apt install sl
    - name: Run sl
      run: sl
    - name: List directory contents
      run: ls -la
